# Verification for #28 Self-hosted deployment AI defaults

- The deployment default is fetched from the server and applied if no user-local settings are saved.
- It can be tested by providing the env vars during the test run and verifying the behavior of `loadAiSettings`.
